# SahamLens Bug List

Status per 2026-08-04, diverifikasi manual satu-satu terhadap kode aktual (bukan asumsi dari laporan audit) sebelum ditandai FIXED/INVALID.

| ID | Severity | Status | Module | Bug | Impact |
|----|----------|------------|--------|-----|--------|
| 1 | P1 | **INVALID (dead code, dihapus)** | Paper Trading | Race Condition saat `buyStock` di `demo-portfolio.ts` | File tidak dipanggil di `app/`/`components/` manapun (0 pemanggil, diverifikasi via grep). Fitur portfolio yang aktif ada di `modules/portfolio`, sudah pakai PostgreSQL transaction + row lock `FOR UPDATE` (`trade.service.ts`) sejak sebelum audit ini. `lib/demo-portfolio.ts` dihapus. |
| 2 | P1 | **INVALID (dead code, dihapus)** | Paper Trading | Missing Input Validation di `buyStock`/`sellStock` | Sama seperti #1 - dead code. Endpoint aktif (`/api/portfolio/buy`, `/sell`) sudah divalidasi `tradeSchema` (Zod): `lots: z.number().int().positive()`, `price: z.number().finite().positive()`. |
| 3 | P1 | **FIXED** | Technical | MACD Histogram `=== 0` dilabel "bearish" | `modules/technical/service/scoring.service.ts` - cabang netral terpisah ditambahkan (`macdHist < 0` vs `=== 0` vs `> 0`), skor 3/7/0. Histogram flat tidak lagi diklaim bearish. |
| 4 | P2 | **FIXED** | Build | Middleware Deprecation | `middleware.ts` → `proxy.ts` sesuai migrasi resmi Next.js 16 (rename file + fungsi, matcher/logic identik). |
| 5 | P2 | **FIXED** | Build | Invalid Config `serverComponentsExternalPackages` | `next.config.mjs` - dipindah ke `serverExternalPackages` (top-level). |
| 6 | P2 | **ACKNOWLEDGED, tidak diubah** | Portfolio | BIGINT Cast Precision Loss (`portfolio.service.ts`) | `Number(portfolio.cash)` kehilangan presisi hanya di atas Rp 9 kuadriliun (`Number.MAX_SAFE_INTEGER`) - tidak realistis untuk portfolio virtual (modal awal jutaan-miliaran). Dibiarkan, bukan prioritas. |
| 7 | P2 | **Diverifikasi: BUKAN bug aktif** | Fundamental | `mos` Edge Case pada DCF (`dcf-valuation.service.ts` baris 231 & 368) | Ditelusuri manual: setiap komponen intrinsic value (PBV/DDM/PER/DCF/Graham) sudah dijaga `> 0` sebelum masuk `fair_value`/`fairValue`, dan `fcf_per_share`/`fcfPerShare` dijaga `> 0` sebelum proyeksi DCF. `fair_value` tidak bisa negatif di kode saat ini - `mos = 0` hanya tercapai saat memang tidak ada satu pun metode valid terhitung ("data tidak cukup"), bukan "overvalued ekstrem". Tidak diubah - tidak ada yang perlu diperbaiki. |
| 8 | P2 | **BY DESIGN, tidak diubah** | Proxy (dulu Middleware) | Rate Limit Bypass on Browser Request | Didokumentasikan eksplisit di `proxy.ts`: navigasi halaman sengaja dibiarkan lewat (shell HTML kosong tidak berharga), data sesungguhnya tetap digerbang lewat rate limit di panggilan `/api/*`-nya sendiri. Keputusan desain lama, bukan bug. |
| 9 | P2 | **BELUM diperbaiki** | Market | Timezone/Volume Bias di `estimateFullDayVolume` (`shared/market/trading-session.ts`) | Estimasi linear (`partialVolume / elapsedFraction`) tidak merefleksikan pola volume intraday IDX yang sesungguhnya non-linear (spike di open & pre-close). Masih berlaku - TIDAK ada perubahan kode di file ini. Perbaikan sebenarnya butuh model distribusi volume historis, bukan fix satu baris - di luar scope perbaikan cepat. |
| 10 | P3 | **FIXED (2026-08-04)** | Fundamental | Fallback 15500 USDIDR (`dcf-valuation.service.ts`) | `getUsdIdrRate()` baru - cache kurs sukses terakhir di Redis (`shared/cache/redis-cache.ts`, TTL 7 hari), dipakai saat fetch Yahoo gagal. 15500 sekarang cuma jaring pengaman TERAKHIR (kalau cache pun kosong, mis. fresh deploy). |
| 11 | P3 | **FIXED** | System | Deprecated Sentry Logger | `disableLogger` dihapus dari `next.config.mjs` - opsi lama & penggantinya (`webpack.treeshake.removeDebugLogging`) sama-sama tidak berlaku di bawah Turbopack (yang dipakai proyek ini), jadi dihapus saja alih-alih diganti opsi yang sama-sama inert. |
| 12 | P3 | **Diverifikasi: BUKAN bug** | Portfolio | "Dead Code" di Portfolio Service | Dicek `transaction.repository.ts` - `listTransactions()` sudah cursor-based, `limit`/`cursor` benar-benar dipakai di query SQL, tidak ada dead code. Temuan awal (P3, confidence rendah) tidak akurat. Tidak diubah. |
