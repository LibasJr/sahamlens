import { useState } from 'react';
import { ChevronDown, Info, LockKeyhole, Plus, Wallet } from 'lucide-react';
import type { Ticker } from '../main';
import { AIInsights } from './AIInsights';
export function OrderPanel({ ticker, apiBaseUrl }: { ticker?: Ticker; apiBaseUrl: string }) {
  const [tab, setTab] = useState('Market'); const [side, setSide] = useState<'buy' | 'sell'>('buy');
  return <aside className="order-panel"><div className="order-profile"><div className="profile-avatar">L</div><div><strong>Personal account</strong><small>Connected to SahamLens API</small></div><ChevronDown size={14} className="muted-icon" /></div>{!ticker ? <div className="order-empty">Pilih saham untuk melihat data akun dan membuat order.</div> : <><div className="account-balance"><div><span>AVAILABLE TO TRADE</span><strong>Data akun memerlukan login</strong></div><Wallet size={17} /></div><div className="position-card"><div><span>POSITION · {ticker.symbol}</span><strong>N/A</strong></div><div className="position-pnl"><span>UNREALIZED P&amp;L</span><b>N/A</b></div></div><div className="order-tabs">{['Market', 'Limit', 'Stop'].map((item) => <button className={tab === item ? 'active' : ''} key={item} onClick={() => setTab(item)}>{item}</button>)}</div><div className="order-form"><label>Symbol <button className="symbol-select">{ticker.symbol}<ChevronDown size={13} /></button></label><label>Quantity <div className="input-wrap"><input placeholder="Enter quantity" /><span>SHARES</span></div></label><div className="estimated"><span>Order routing <Info size={12} /></span><strong>Login required</strong></div><div className="side-buttons"><button className="buy" onClick={() => setSide('buy')}>Buy {ticker.symbol}</button><button className="sell" onClick={() => setSide('sell')}>Sell {ticker.symbol}</button></div><div className="secure-note"><LockKeyhole size={12} /> Live orders require authenticated broker integration</div><small className="api-note">{apiBaseUrl.replace('https://', '')}</small></div><div className="orderbook"><div className="subsection-heading"><span>ORDER BOOK</span></div><div className="order-empty compact">Order book tidak tersedia dari API publik.</div></div><AIInsights /></>}</aside>;
}
