SahamLens Transparency Production Build Fix

Masalah:
- /transparency diprerender saat next build.
- import repository market-data langsung menarik postgres.client.
- CI build tidak memiliki DATABASE_URL, sehingga prerender gagal.

Fix:
- force dynamic rendering untuk /transparency.
- lazy import repository hanya saat request runtime.
- reconciliation section fail-soft jika DB sementara unavailable.
- TIDAK menambahkan DATABASE_URL ke GitHub Actions.

File:
app/transparency/page.tsx
